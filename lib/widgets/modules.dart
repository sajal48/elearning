import 'package:flutter/material.dart';

class ModuleSection extends StatelessWidget {
  const ModuleSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
